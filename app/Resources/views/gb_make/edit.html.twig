{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gb_make edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('gb_make_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
{% include 'nav_admin.html.twig' %}


<div class="home-section" style="padding-top:150px;">    
  <div class="container">

    <div class="row">

      <div class="col-md-12 col-sm-12 col-md-12">
        <div class="panel-body">

            <div id="errormessage"></div>

            {{ form_start(edit_form, {'attr': {'class': 'contactForm lead', 'id': 'purchace_form', 'role': 'form'}}) }}

            <div class="row">
                <div class="col-xs-6 col-sm-6 col-md-6">
                <div class="form-group">
                <label for="sel1">Make:</label>
                {{ form_widget(edit_form.mName, { 'attr': {'placeholder': 'Car Make'|trans, 'class': 'form-control' }}) }}
                {{ form_errors(edit_form.mName, { 'attr': {'class': 'validation'}} ) }}
                </div> 
                </div> 

            </div>

            <input type="submit" value="Create" class="btn btn-skin btn-lg">

            {{ form_end(edit_form) }}
        </div>
      </div>

    </div>
    <ul>
        <li>
            <a href="{{ path('gb_make_index') }}">Back to the list</a>
        </li>
    </ul>

    

  </div>
</div>

{% endblock %}
